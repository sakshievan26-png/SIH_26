[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "sih-member5-audit"
version = "0.2.0"
requires-python = ">=3.10"
description = "Independent SIH audit journal and EVM anchoring module"

[tool.setuptools.packages.find]
include = ["member5*"]
